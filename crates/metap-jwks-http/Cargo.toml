[package]
name = "metap-jwks-http"
version = "0.1.0"
edition.workspace = true

[dependencies]
metap-jwks = { path = "../metap-jwks" }
axum.workspace = true
tokio = { workspace = true, features = ["sync"] }

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread", "net"] }
reqwest.workspace = true
