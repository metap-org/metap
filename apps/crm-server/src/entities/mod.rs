//! Entity definitions for `apps/crm-server`'s demo CRM app — see this crate's own doc comment
//! in `../main.rs` and `docs/features/README.md` for why these are demo/proof entities, not a
//! real business module set.

pub mod customer_entity;
pub mod inventory_movement_entity;
pub mod journal_entry_entity;
pub mod sales_order_entity;
