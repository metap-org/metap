//! Entity definitions for `apps/jira-server` — see `../main.rs`'s doc comment for why this app
//! exists (table-per-entity, wired into a real boot sequence for the first time).

pub mod issue_entity;
pub mod project_entity;
